Index: llvm-toolchain-snapshot_21~++20250307093406+951353de62c3/clang/lib/Driver/ToolChains/WebAssembly.cpp
===================================================================
--- llvm-toolchain-snapshot_21~++20250307093406+951353de62c3.orig/clang/lib/Driver/ToolChains/WebAssembly.cpp
+++ llvm-toolchain-snapshot_21~++20250307093406+951353de62c3/clang/lib/Driver/ToolChains/WebAssembly.cpp
@@ -21,6 +21,11 @@
 #include "llvm/Support/Path.h"
 #include "llvm/Support/VirtualFileSystem.h"
 
+#include "llvm/Config/llvm-config.h" // for LLVM_VERSION_MAJOR
+
+#define TOSTR2(X) #X
+#define TOSTR(X) TOSTR2(X)
+
 using namespace clang::driver;
 using namespace clang::driver::tools;
 using namespace clang::driver::toolchains;
@@ -272,7 +277,7 @@ WebAssembly::WebAssembly(const Driver &D
 const char *WebAssembly::getDefaultLinker() const {
   if (TargetBuildsComponents(getTriple()))
     return "wasm-component-ld";
-  return "wasm-ld";
+  return "wasm-ld-" TOSTR(LLVM_VERSION_MAJOR);
 }
 
 bool WebAssembly::IsMathErrnoDefault() const { return false; }
